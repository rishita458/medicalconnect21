# MedConnect Backend

Setup:
1. Copy .env.example to .env and set MONGO_URI and JWT_SECRET.
2. npm install
3. npm run seed (optional) to create sample users
4. npm run dev

Notes:
- Tests: npm test
- Replace MONGO_URI with MongoDB Atlas connection string when deploying.
